#include <iostream>
#include <ctime>
#include <cstdlib>
#include <sstream>

int nbIP();
std::string IP();
std::string IPp();
bool chance(int max);
std::string corruption(std::string _texte,bool &corruption);